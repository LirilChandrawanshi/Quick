spring.application.name=Quick
# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=quickcommerce
# Error handling configuration
server.error.whitelabel.enabled=false
server.error.include-stacktrace=never
server.error.include-message=always
# Server configuration
server.port=8080

# Allow bean definition overriding
spring.main.allow-bean-definition-overriding=true

# Standard Spring Boot and Actuator settings
management.endpoints.web.exposure.include=health,info,metrics
logging.level.org.springframework.data=info
logging.level.org.springframework.web=info

# API Key for security
api.key=api-key-secret

# Server configuration
server.port=8080

# Allow bean definition overriding
spring.main.allow-bean-definition-overriding=true
# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=quickcommerce
# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/quickcommerce
spring.data.mongodb.auto-index-creation=true

# JWT Configuration
jwt.secret=YQZEMQZFTnOuVhp0fVSKdnNyaHzQ5O7yg9zcnZ/s/j8=
jwt.expiration=86400000

# Error handling configuration
server.error.whitelabel.enabled=false
server.error.include-stacktrace=never
server.error.include-message=always

# Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui

# Application configuration
spring.application.name=Quick Commerce API
# SpringDoc OpenAPI/Swagger Configuration
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.syntaxHighlight.activated=true

# Application Configuration
api.key=your-api-key-here

# Actuator Configuration (for monitoring)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH
spring.web.cors.allowed-headers=*
# Database Configuration
spring.datasource.url=jdbc:h2:mem:quickdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

